{
  "name": "buildrail",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source governance for AI-assisted software development.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@buildrail/cli",
    "test": "npm run test --workspace=@buildrail/cli",
    "lint": "echo \"BuildRail linting not configured yet\"",
    "typecheck": "npm run typecheck --workspace=@buildrail/cli"
  },
  "license": "Apache-2.0"
}
